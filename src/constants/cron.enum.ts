export enum ScheduleEnum {
  DAILY = 'DAILY',
  WEEKLY = 'WEEKLY',
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY'
}

export enum ScheduleEndEnum {
  NEVER = 'NEVER',
  ON = 'ON',
  AFTER = 'AFTER',
}
